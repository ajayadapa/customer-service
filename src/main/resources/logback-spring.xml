<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <statusListener class="ch.qos.logback.core.status.OnConsoleStatusListener"/>
    <!-- ================= Spring Properties ================= -->
    <springProperty scope="context"
                    name="SERVICE_NAME"
                    source="spring.application.name"/>

    <!-- ================= Log Directory ================= -->
    <property name="LOG_BASE" value="C:/Users/ajay.k.adapa/Digital_Bank_Project/logs"/>

    <!-- ================= Log Pattern ================= -->
    <property name="LOG_PATTERN"
              value="%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} -
traceId=%X{trace_id:-} spanId=%X{span_id:-} %msg%n"/>

    <!-- ================= Console Appender ================= -->
    <appender name="CONSOLE"
              class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>${LOG_PATTERN}</pattern>
        </encoder>
    </appender>

    <!-- ================= File Appender ================= -->
    <appender name="FILE"
              class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_BASE}/${SERVICE_NAME}/${SERVICE_NAME}.log</file>

        <rollingPolicy
                class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>
                ${LOG_BASE}/${SERVICE_NAME}/${SERVICE_NAME}.%d{yyyy-MM-dd}.log
            </fileNamePattern>
            <maxHistory>30</maxHistory>
        </rollingPolicy>

        <encoder>
            <pattern>${LOG_PATTERN}</pattern>
        </encoder>
    </appender>

    <!-- ================= Log Level Overrides ================= -->
    <!-- Hibernate noise reduction -->
    <logger name="org.hibernate" level="WARN"/>

    <!-- Spring framework logs -->
    <logger name="org.springframework" level="INFO"/>

    <!-- ================= Root Logger ================= -->
    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="FILE"/>
    </root>

</configuration>
